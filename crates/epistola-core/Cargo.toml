[package]
name = "epistola-core"
description = "Core domain types and traits for epistola: Request, Response, and the extension points (HttpExecutor, VariableResolver) other crates implement."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true

[dependencies]
thiserror.workspace = true
minijinja = { version = "2", default-features = false, features = ["std_collections", "serde"] }

[lints]
workspace = true
